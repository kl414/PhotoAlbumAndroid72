<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kevinlin.photoalbum" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".util.Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".util.AdminView"
            android:label="@string/title_activity_admin_view"
            android:parentActivityName=".util.Login" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.kevinlin.photoalbum.util.Login" />
        </activity>
        <activity
            android:name=".util.Userview"
            android:label="@string/title_activity_userview"
            android:parentActivityName=".util.Login" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.kevinlin.photoalbum.util.Login" />
        </activity>
    </application>

</manifest>
